#include<iostream>
using namespace std; //if not used we have to write std::

int main(){
    cout<<"hello world!"<<endl; /*can use \n rather than endl*/
    cout<<"Next line\n";

    int a,b;
    cout<<"enter first number: ";
    cin>>a;
    cout<<"enter second number: ";
    cin>>b;
    return 0;
}